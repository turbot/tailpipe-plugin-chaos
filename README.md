# tailpipe-plugin-chaos

Tailpipe plugin to test the Tailpipe CLI and the plugin SDK features.

## Developers

Build a developer version of the plugin:
```
make
```

Check it was created and installed:
```
ls -al ~/.tailpipe/plugins
```
